# MCP Server (runs on DGX Spark)
MCP_PORT=3100

# Ollama (runs on host, accessed via host networking)
OLLAMA_HOST=localhost:11434

# vLLM defaults
VLLM_IMAGE=nvcr.io/nvidia/vllm:latest
VLLM_PORT=8000

# GPU memory management
VLLM_GPU_MEMORY_UTILIZATION=0.7
